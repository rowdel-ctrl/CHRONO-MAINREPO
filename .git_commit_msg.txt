Add HANDOFF.md for session context handoff between Claude chat and Claude Code

## Summary
Created HANDOFF.md template and initial baseline entry documenting current ChronoQuest
codebase status. This file serves as persistent context bridge between conversation
sessions, enabling continuity without re-reading full conversation history.

## What changed
- Created HANDOFF.md with template structure + first dated entry (2026-09-19)
- Baseline assessment: Game camera/world rebuild (Phases 0–5) complete, parallax
  regression fixed, 8/8 tests passing, all core systems stable
- Documented next priority: question randomization for repeat-play
- Identified deferred refactor: chrono_game.dart over budget (388 lines) but safe to
  defer until after randomization feature

## Decisions documented
1. Why chrono_game.dart refactor was deferred (scope creep risk, no dependency)
2. Why parallax uses velocity scaling (preserves pre-Phase-3 visual speed)
3. Why tests use logic-only approach (flame_test version conflict workaround)
4. What failed in Phase 3–4 and how it was fixed (parallax speed, level-end check)

## For future sessions
- Start session: Read top entry of HANDOFF.md + CLAUDE.md
- End session: Add new dated entry (goal, state, decisions, next steps, constraints)
- Once 4+ entries exist: Fold oldest to one-line summary at bottom

See CAMERA_REBUILD_CHECKLIST.md for full Phase 0–5 audit trail.

